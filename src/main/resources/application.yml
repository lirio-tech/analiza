
spring:
  data:
    mongodb:
      authentication-database: admin
      username: auction
      password: auctiondb123
      database: auctiondb
      port: 27017
      host: localhost
      auto-index-creation: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER}
    password: ${MAIL_PASS}
    properties.mail.smtp:
      auth: true
      starttls.enable: true
