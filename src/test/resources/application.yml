spring:
  profiles.active: default
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/auctiondb}
      #database: employee-service-blog
      #host: employeemongodb
      #port: 27017
